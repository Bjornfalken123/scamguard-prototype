const $ = (s) => document.querySelector(s);
let state = JSON.parse(localStorage.getItem('sg-state') || '{}');
state = { senior:'Ingrid', protection:true, family:[{name:'Björn', role:'Son', phone:'+46 70 123 45 67', alerts:true},{name:'Maria', role:'Dotter', phone:'+46 72 222 33 44', alerts:true}], trusted:['Vårdcentralen','Hemtjänsten','Familj'], ...state };
const save=()=>localStorage.setItem('sg-state', JSON.stringify(state));
const calls=[
{id:'c1',name:'Okänt nummer',from:'+46 73 402 18 91',risk:94,type:'bad',label:'Blockerat',time:'Idag 14:22',summary:'Påstod sig ringa från banken och bad om BankID. AI stoppade samtalet och varnade anhörig.',reasons:['Bad om BankID','Skapade tidspress','Ville att Ingrid skulle logga in']},
{id:'c2',name:'Vårdcentralen',from:'+46 8 555 102 10',risk:12,type:'good',label:'Släpptes igenom',time:'Idag 10:04',summary:'Identifierade sig tydligt och ärendet var en bokad tid. Samtalet kopplades vidare.',reasons:['Tydlig avsändare','Ingen ekonomisk begäran','Låg risk']},
{id:'c3',name:'Okänt utlandssamtal',from:'+44 20 7946 0412',risk:71,type:'warn',label:'Krävde bekräftelse',time:'Igår 17:41',summary:'Oklart ärende och hög press. Appen bad anhörig granska innan vidarekoppling.',reasons:['Okänt utlandsnummer','Otydligt ärende','Hög press']}
];
function chip(call){return `<span class="chip ${call.type}">${call.label} · ${call.risk}%</span>`}
function shell(content, screen='home'){return `<main class="phone"><div class="top"><div class="brand"><div class="logo">✓</div>ScamGuard</div><span class="pill">Prototyp</span></div>${content}</main><nav class="nav"><button class="${screen==='home'?'active':''}" onclick="go('home')">Skydd</button><button class="${screen==='calls'?'active':''}" onclick="go('calls')">Samtal</button><button class="${screen==='family'?'active':''}" onclick="go('family')">Anhöriga</button><button class="${screen==='settings'?'active':''}" onclick="go('settings')">Regler</button></nav><div id="modal" class="modal"></div>`}
function renderHome(){app.innerHTML=shell(`<section class="hero"><div class="statusrow"><div><span class="chip good">Skydd aktivt</span><h1>Okända samtal screenas först</h1><p>${state.senior} behöver inte svara på misstänkta samtal. AI:n tar första samtalet och kopplar bara vidare när det verkar tryggt.</p></div><div class="shield">🛡</div></div><div class="grid"><div class="metric"><b>3</b><span>samtal screenade</span></div><div class="metric"><b>1</b><span>bedrägeri stoppat</span></div></div></section><h2>Så fungerar skyddet</h2><div class="card timeline"><div class="step"><b>Okänt nummer ringer</b><div class="small">Samtalet går först till ScamGuard-numret.</div></div><div class="step"><b>AI frågar vad samtalet gäller</b><div class="small">Bedömer avsändare, ärende, press och riskfraser.</div></div><div class="step"><b>Trygga samtal kopplas vidare</b><div class="small">Riskfyllda samtal stoppas och anhörig får varning.</div></div></div><button class="btn" onclick="simulate()">Simulera okänt samtal</button><div class="notice" style="margin-top:12px">I denna prototyp är telefoni och AI mockade. Flödet visar hur den färdiga produkten ska kännas.</div>`, 'home')}
function renderCalls(){app.innerHTML=shell(`<h2>Samtalsrapport</h2><div class="stack">${calls.map(c=>`<div class="card call ${c.type}" onclick="openCall('${c.id}')"><div class="row"><div><div class="title">${c.name}</div><div class="small">${c.from} · ${c.time}</div></div>${chip(c)}</div><p class="small">${c.summary}</p></div>`).join('')}</div>`, 'calls')}
function renderFamily(){app.innerHTML=shell(`<h2>Anhöriga</h2><p class="small">Anhöriga får varning när ett samtal blockeras eller behöver granskas.</p>${state.family.map((f,i)=>`<div class="card"><div class="row"><div><div class="title">${f.name}</div><div class="small">${f.role} · ${f.phone}</div></div><div class="toggle"></div></div></div>`).join('')}<div class="card"><div class="stack"><input id="fname" placeholder="Namn"><input id="fphone" placeholder="Telefonnummer"><button class="btn secondary" onclick="addFamily()">Lägg till anhörig</button></div></div>`, 'family')}
function renderSettings(){app.innerHTML=shell(`<h2>Samtalsregler</h2><div class="card"><div class="setting row"><div><b>Okända nummer</b><div class="small">Screenas av AI innan de kopplas vidare.</div></div><div class="toggle"></div></div><div class="setting row"><div><b>Kontakter</b><div class="small">Släpps igenom direkt.</div></div><div class="toggle"></div></div><div class="setting row"><div><b>Hög risk</b><div class="small">Blockera samtalet och varna anhörig.</div></div><div class="toggle"></div></div><div class="setting row"><div><b>Osäkra samtal</b><div class="small">Be anhörig granska innan vidarekoppling.</div></div><div class="toggle"></div></div></div><h2>Koppla samtal</h2><div class="card"><p class="small">I färdig version hjälper appen användaren att aktivera vidarekoppling av okända samtal till ScamGuard. I prototypen visas bara flödet.</p><div class="mono">Seniorens nummer → ScamGuard AI → Senior / Blockera / Anhörig</div><br><button class="btn warn" onclick="showSetup()">Visa kopplingsflöde</button></div>`, 'settings')}
function addFamily(){const n=$('#fname').value.trim(),p=$('#fphone').value.trim(); if(!n||!p)return; state.family.push({name:n,role:'Anhörig',phone:p,alerts:true}); save(); renderFamily()}
function openCall(id){const c=calls.find(x=>x.id===id); $('#modal').innerHTML=`<div class="sheet"><div class="row"><h2>${c.name}</h2>${chip(c)}</div><div class="small">${c.from} · ${c.time}</div><p>${c.summary}</p><b>AI hittade:</b><ul>${c.reasons.map(r=>`<li>${r}</li>`).join('')}</ul><button class="btn" onclick="closeModal()">Stäng</button></div>`; $('#modal').classList.add('show')}
function simulate(){const c=calls[0]; $('#modal').innerHTML=`<div class="sheet"><span class="chip bad">Inkommande okänt samtal</span><h2>AI screenar samtalet</h2><div class="timeline"><div class="step"><b>Uppringaren säger:</b><div class="small">“Jag ringer från banken, du behöver öppna BankID.”</div></div><div class="step"><b>AI-risk: 94%</b><div class="small">Matchar kända bedrägerimönster.</div></div><div class="step"><b>Beslut</b><div class="small">Samtalet blockeras. Anhörig varnas.</div></div></div><button class="btn" onclick="go('calls')">Visa rapport</button></div>`; $('#modal').classList.add('show')}
function showSetup(){ $('#modal').innerHTML=`<div class="sheet"><h2>Aktivera samtalsskydd</h2><div class="timeline"><div class="step"><b>1. Bekräfta seniorens nummer</b><div class="small">Appen visar vilket nummer som ska skyddas.</div></div><div class="step"><b>2. Aktivera vidarekoppling</b><div class="small">Guiden hjälper användaren steg för steg.</div></div><div class="step"><b>3. Testa skyddet</b><div class="small">ScamGuard ringer ett testsamtal och visar att allt fungerar.</div></div></div><button class="btn" onclick="closeModal()">Klart</button></div>`; $('#modal').classList.add('show') }
function closeModal(){ $('#modal').classList.remove('show') }
function go(s){ closeModal?.(); if(s==='home')renderHome(); if(s==='calls')renderCalls(); if(s==='family')renderFamily(); if(s==='settings')renderSettings(); }
window.go=go;window.simulate=simulate;window.openCall=openCall;window.closeModal=closeModal;window.addFamily=addFamily;window.showSetup=showSetup;const app=document.getElementById('app');renderHome();
if('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js').catch(()=>{});
